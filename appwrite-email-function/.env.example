# Gmail SMTP Configuration for ONSi Email Function
# Copy this file to .env and fill in your actual values

# Gmail SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-gmail@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Email Recipients
SUBMIT_EMAIL=admin@yourstore.com

# Optional Settings (defaults will be used if not specified)
# SMTP_SECURE=false
# SMTP_REQUIRE_TLS=true

# Instructions:
# 1. Replace 'your-gmail@gmail.com' with your actual Gmail address
# 2. Replace 'your-app-specific-password' with an App Password from your Gmail account
#    - Go to Google Account Settings > Security > 2-Step Verification > App Passwords
#    - Generate an App Password for "Mail" application
# 3. Replace 'admin@yourstore.com' with the email where you want to receive order notifications
# 4. Save this file as '.env' in the same directory as main.js